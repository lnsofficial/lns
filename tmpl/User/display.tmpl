<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
    <title>LNS</title>
    <!-- Bootstrap -->
	<link href="/css/bootstrap.css" rel="stylesheet">
	<link href="/css/style.css" rel="stylesheet" type="text/css">

	<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
	<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
	<!--[if lt IE 9]>
		  <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
		  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
		<![endif]-->
  <!--The following script tag downloads a font from the Adobe Edge Web Fonts server for use within the web page. We recommend that you do not modify it.--><script>var __adobewebfontsappname__="dreamweaver"</script><script src="http://use.edgefonts.net/open-sans:n3,n8,n4:default;overlock:n4:default;poiret-one:n4:default;paytone-one:n4:default.js" type="text/javascript"></script>
</head>
  <body>
  {include file='header.tmpl'}
  <div class="container-fluid">

      <div class="row">
        <div class="col-lg-2 about-menu">
          <ul class="nav side">
            <li role="presentation">ユーザー詳細</li>
          </ul>
        </div>
        <div class="col-lg-9">
          <h2>ユーザー情報</h2>
          <table class="table-bordered">
            <tr>
              <th>サモナー名</th>
              <td>{$summoner_name}</td>
            </tr>
            <tr>
              <th>メインロール</th>
              <td>
                {if $main_role == 1}Top{/if}
                {if $main_role == 2}Jungle{/if}
                {if $main_role == 3}Mid{/if}
                {if $main_role == 4}ADC{/if}
                {if $main_role == 5}Support{/if}
              </td>
            </tr>
          </table>


          <h3>申請一覧</h3>
          <table  class="table-bordered">
            <tr>
              <th>id</th>
              <th>team_id</th>
              <th>team_name</th>
              <th>user_id</th>
              <th>type</th>
              <th>state</th>
              <th>自分の時だけ出るぼたん</th>
            </tr>
            {foreach from=$user_team_applys item=apply key=idx}
              <tr>
                <td>{$apply.id}</td>
                <td>{$apply.team_id}</td>
                <td>{$apply.team_name}</td>
                <td>{$apply.user_id}</td>
                <td>{$apply.type}</td>
                <td>{$apply.state}</td>
                <td>
                <form method="post" action="/User/apply_cancel" class="form-horizontal">
                  <input type="hidden" name="user_team_apply_id" value="{$apply.id}">
                  <button type="submit" class="btn btn-default">申請をキャンセルするボタン</button>
                </form>
                </td>
              </tr>
            {/foreach}
          </table>


        </div>
      </div>

  </div>
  {include file='footer.tmpl'}
<!-- jQuery (necessary for Bootstrap's JavaScript plugins) --> 
  <script src="/js/jquery-1.11.3.min.js"></script>
  <!-- Include all compiled plugins (below), or include individual files as needed --> 
  <script src="/js/bootstrap.js"></script>
  </body>
</html>
